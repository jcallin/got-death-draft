{
  "name": "got-death-draft",
  "version": "1.0.0",
  "scripts": {
    "start": "npm run build-client && node server",
    "start-local": "npm run build-client && PORT=8080 node server",
    "build-client": "cd client && npm run build && cd .."
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.16.2",
    "mongodb": "^3.2.3",
    "mongoose": "^5.5.4",
    "morgan": "^1.9.1"
  }
}
